# tmpfiles.d config for AgenC directories
# Agent runs as root — this is a dedicated device, not a shared server
d /data 0755 root root -
d /data/agenc 0750 root root -
d /data/keystore 0700 root root -
d /data/agenc/logs 0750 root root -
d /data/agenc/dropbear 0700 root root -
