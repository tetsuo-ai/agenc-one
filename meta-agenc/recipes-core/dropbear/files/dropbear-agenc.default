# AgenC OS Dropbear configuration
# Override default -w flag to allow root SSH login
# Keys are generated in /etc/dropbear via standard dropbearkey.service
# (volatile-binds makes /etc/dropbear writable on read-only rootfs)
DROPBEAR_EXTRA_ARGS=""
